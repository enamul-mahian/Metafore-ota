@extends('layouts.admin')
@section('title', 'Institutions')
@section('content')
<style>.ins-head{display:flex;justify-content:space-between;gap:16px;margin-bottom:18px}.ins-muted{color:#64748b}.ins-filter{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:18px}.ins-filter input,.ins-filter select{border:1px solid #cbd5e1;border-radius:8px;padding:9px}.ins-table{width:100%;border-collapse:collapse;font-size:14px}.ins-table th,.ins-table td{padding:11px 8px;border-bottom:1px solid #e5e7eb;text-align:left}.ins-table th{font-size:12px;text-transform:uppercase;color:#64748b}.ins-link{color:#244fc7;text-decoration:none;font-weight:600}</style>
<div class="ins-head"><div><h1 style="margin:0">Institutions</h1><p class="ins-muted">Manage education institution contact profiles.</p></div>@can('institutions.manage')<a class="egh-button" href="{{ route('admin.institutions.create') }}">Create Institution</a>@endcan</div>
<form method="GET" class="egh-card ins-filter"><input name="search" value="{{ $filters['search'] }}" placeholder="Name, email or registration"><select name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected($filters['status'] === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="egh-button">Filter</button></form>
<div class="egh-card" style="overflow:auto"><table class="ins-table"><thead><tr><th>Name</th><th>Contact</th><th>Registration</th><th>Country</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($institutions as $institution)<tr><td>{{ $institution->name }}</td><td>{{ $institution->email ?: '—' }}@if($institution->phone)<br><span class="ins-muted">{{ $institution->phone }}</span>@endif</td><td>{{ $institution->registration_number ?: '—' }}</td><td>{{ $institution->country ? $institution->country->name.' ('.$institution->country->iso2.')' : '—' }}</td><td>{{ ucfirst($institution->status) }}</td><td><a class="ins-link" href="{{ route('admin.institutions.show', $institution) }}">View</a>@can('institutions.manage') · <a class="ins-link" href="{{ route('admin.institutions.edit', $institution) }}">Edit</a>@endcan</td></tr>@empty<tr><td colspan="6">No institutions found.</td></tr>@endforelse</tbody></table></div><div style="margin-top:16px">{{ $institutions->links() }}</div>
@endsection
