@if($errors->any())<div class="egh-alert" style="background:#fee2e2;color:#991b1b"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<div class="ins-form-grid"><label>Name<input name="name" value="{{ old('name', $institution->name ?? '') }}" required maxlength="200"></label><label>Email<input type="email" name="email" value="{{ old('email', $institution->email ?? '') }}" maxlength="255"></label><label>Phone<input name="phone" value="{{ old('phone', $institution->phone ?? '') }}" maxlength="32"></label><label>Website<input type="url" name="website_url" value="{{ old('website_url', $institution->website_url ?? '') }}" maxlength="2048"></label><label>Registration number<input name="registration_number" value="{{ old('registration_number', $institution->registration_number ?? '') }}" maxlength="100"></label><label>Country<select name="country_id"><option value="">Not specified</option>@foreach($countries as $country)<option value="{{ $country->id }}" @selected((string) old('country_id', $institution->country_id ?? '') === (string) $country->id)>{{ $country->name }} ({{ $country->iso2 }})</option>@endforeach</select></label><label>Status<select name="status">@foreach($statuses as $status)<option value="{{ $status }}" @selected(old('status', $institution->status ?? \App\Models\Institution::STATUS_ACTIVE) === $status)>{{ ucfirst($status) }}</option>@endforeach</select></label><label class="ins-full">Address<textarea name="address" maxlength="500">{{ old('address', $institution->address ?? '') }}</textarea></label><label class="ins-full">Internal notes<textarea name="notes" maxlength="5000">{{ old('notes', $institution->notes ?? '') }}</textarea></label></div><div style="display:flex;gap:10px;margin-top:18px"><button class="egh-button">{{ $submitLabel }}</button><a class="egh-button secondary" href="{{ route('admin.institutions.index') }}">Cancel</a></div>
