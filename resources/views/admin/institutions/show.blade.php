@extends('layouts.admin')
@section('title', $institution->name)
@section('content')
<style>.ins-show{display:grid;grid-template-columns:180px 1fr;gap:12px;margin-top:20px}.ins-show dt{color:#64748b}.ins-show dd{margin:0;word-break:break-word}.ins-pre{white-space:pre-wrap}@media(max-width:650px){.ins-show{grid-template-columns:1fr}}</style><div class="egh-card"><div style="display:flex;justify-content:space-between;gap:16px"><div><h1 style="margin:0">{{ $institution->name }}</h1><p>Institution profile</p></div><div><a class="egh-button secondary" href="{{ route('admin.institutions.index') }}">Back</a> @can('institutions.manage')<a class="egh-button" href="{{ route('admin.institutions.edit', $institution) }}">Edit</a>@endcan</div></div><dl class="ins-show"><dt>Email</dt><dd>{{ $institution->email ?: 'Not specified' }}</dd><dt>Phone</dt><dd>{{ $institution->phone ?: 'Not specified' }}</dd><dt>Website</dt><dd>@if($institution->website_url)<a href="{{ $institution->website_url }}" rel="noopener noreferrer">{{ $institution->website_url }}</a>@else Not specified @endif</dd><dt>Registration</dt><dd>{{ $institution->registration_number ?: 'Not specified' }}</dd><dt>Country</dt><dd>{{ $institution->country ? $institution->country->name.' ('.$institution->country->iso2.')' : 'Not specified' }}</dd><dt>Status</dt><dd>{{ ucfirst($institution->status) }}</dd><dt>Address</dt><dd class="ins-pre">{{ $institution->address ?: 'Not specified' }}</dd><dt>Notes</dt><dd class="ins-pre">{{ $institution->notes ?: 'None' }}</dd></dl>@can('institutions.manage')<form method="POST" action="{{ route('admin.institutions.destroy', $institution) }}" style="margin-top:22px" onsubmit="return confirm('Delete this institution profile?')">@csrf @method('DELETE')<button class="egh-button" style="background:#b91c1c">Delete Institution</button></form>@endcan</div>
@endsection
