@extends('layouts.admin')
@section('title', $institution->name)
@section('content')
<x-admin.page-header :title="$institution->name" description="Review this education institution profile." icon="I" eyebrow="Institution details"><a class="egh-button secondary" href="{{ route('admin.institutions.index') }}">Back</a> @can('institutions.manage')<a class="egh-button" href="{{ route('admin.institutions.edit', $institution) }}">Edit</a>@endcan</x-admin.page-header>
<div class="egh-card"><dl class="ins-show"><dt>Email</dt><dd>{{ $institution->email ?: 'Not specified' }}</dd><dt>Phone</dt><dd>{{ $institution->phone ?: 'Not specified' }}</dd><dt>Website</dt><dd>@if($institution->website_url)<a href="{{ $institution->website_url }}" rel="noopener noreferrer">{{ $institution->website_url }}</a>@else Not specified @endif</dd><dt>Registration</dt><dd>{{ $institution->registration_number ?: 'Not specified' }}</dd><dt>Country</dt><dd>{{ $institution->country ? $institution->country->name.' ('.$institution->country->iso2.')' : 'Not specified' }}</dd><dt>Status</dt><dd>{{ ucfirst($institution->status) }}</dd><dt>Address</dt><dd class="ins-pre">{{ $institution->address ?: 'Not specified' }}</dd><dt>Notes</dt><dd class="ins-pre">{{ $institution->notes ?: 'None' }}</dd></dl>@can('institutions.manage')<form method="POST" action="{{ route('admin.institutions.destroy', $institution) }}" class="admin-danger-zone" onsubmit="return confirm('Delete this institution profile?')">@csrf @method('DELETE')<button class="egh-button danger">Delete Institution</button></form>@endcan</div>
@endsection
